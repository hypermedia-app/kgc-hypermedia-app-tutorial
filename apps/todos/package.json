{
  "name": "@creta-todos/todos",
  "version": "0.0.0",
  "private": "true",
  "scripts": {
    "bootstrap": "talos put --api $TODOS_BASE --endpoint $SPARQL_ENDPOINT --user $SPARQL_USER --password $SPARQL_PASSWORD",
    "build": "tsc"
  },
  "dependencies": {
    "@creta-todos/auth": "0.0.0",
    "@creta-todos/domain": "0.0.0",
    "@hydrofoil/knossos": "^0.6.1"
  },
  "devDependencies": {
    "@hydrofoil/talos": "^0.4.4"
  }
}
